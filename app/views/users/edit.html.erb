<%= form_with model: @user, local: true do |f| %>
  <%= f.label :avatar %>
  <%= f.file_field :avatar %>

  <%= f.label :name, 'Display name' %>
  <%= f.text_field :name %>

  <%= f.label :summary %>
  <%= f.text_area :summary %>

  <%= f.submit 'Update profile' %>
<% end %>
